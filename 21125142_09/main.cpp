#include <bits/stdc++.h>
using namespace std;
#include "function.h"

int main(){
    List lst,lst1;
    init(lst); init(lst1);
    Input(lst); Input(lst1);
    Join_two_lists(lst,lst1);
    display(lst);
}